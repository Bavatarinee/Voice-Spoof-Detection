# Voice-Spoof-Detection
A deep-learning powered web application that detects whether a voice recording is genuine (real) or AI-generated / spoofed, using a Convolutional Neural Network (CNN) trained on MFCC audio features.
